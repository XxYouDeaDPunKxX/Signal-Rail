You are localizing a fresh international Signal Rail kit into one personal language version.

This is a first-language setup task, not a live-instance translation task.

Execution model:
- You perform the semantic localization work.
- `audit_localization.ps1` is a final validator for the shipped international English baseline profile, not a localizer.

Rules:
- Work from a fresh international SR kit into a separate target copy.
- If source path and target path are the same, stop and refuse to edit.
- Do not localize host-project content.
- Do not relocalize an already active instance.
- Keep all file names unchanged.
- Keep all ID families unchanged.
- Keep `Signal Rail` as the system name.
- Keep `AI_TO_AI__DEPLOYED_INSTANCE_SIGNAL_RAIL.txt` as the marker file name.
- Keep the canonical structure unchanged.

Localize:
- `00_runtime_entry.txt`
- canonicals
- shipped lateral support files that belong to the SR core, such as `97_field_findings.txt`
- AI QUICK NOTE blocks
- AI SECTION blocks
- section titles
- template labels
- form tags
- state labels
- launcher prompts and messages
- protocol wording where canonical field labels changed

Do not localize by default:
- this `SR Localization Kit` support folder
- validator internals
- baseline support tooling used only to perform localization

Do not invent local synonyms file by file.
Use one stable vocabulary across the whole kit.
If the vocabulary is not locked, stop before editing.

Work order:
1. Confirm the source baseline path and the separate target-copy path.
2. Lock the vocabulary first.
3. Localize `00_runtime_entry.txt`.
4. Localize canonicals and shipped lateral support files.
5. Sync `06`.
6. Sync `07`.
7. Review the marker text if needed.
8. Update launcher prompts and bootstrap placeholders.
9. Run a final anti-mixed-language audit.
10. If `audit_localization.ps1` is available, run it on the target copy. Otherwise tell the user to run it.

Final output must confirm:
- source path
- target path
- which files were changed
- whether vocabulary was locked before edits
- whether any structural term still feels unsafe
- whether bootstrap placeholders were updated
- whether the kit is now monolingual and structurally aligned
- validator result, or that the validator still needs to be run
